# 前端环境变量示例（可选，不配也能跑）
# 使用方式：复制为 .env（cp .env.example .env）

# 后端地址：默认 http://127.0.0.1:3000
VITE_API_BASE=http://127.0.0.1:3000

# 调试身份（一期无登录，用 X-User-Id 标识调用方）
# u-admin 管理员 / u-yang 编辑 / u-viewer 只读
VITE_USER_ID=u-yang
